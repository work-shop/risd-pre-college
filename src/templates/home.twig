{% extends "partials/base.twig" %}

{% import "home/home-macros.twig" as home_macros %}

{% block content %}

<section class="home-page-slideshow">
    {{ home_macros.slideshow( options.home_page_slideshow ) }}
</section>

<section class="home-page-quicklinks container pt2">
    {{ home_macros.quicklinks( options.home_page_program_overview_text, options.home_page_info_text, options.home_page_callout_text  ) }}
</section>

{% endblock %}
